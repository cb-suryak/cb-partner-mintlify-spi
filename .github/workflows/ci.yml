name: CI Workflow

on:
  pull_request:
    types: [closed]
    branches: ["dev", "main"]
  push:
    branches:
      - 'release/*'

jobs:
  build:
    uses: XXXXX
    secrets: inherit
    with:
      CI_ROLE: XXXXX
      GENERATE_OPEN_API: true